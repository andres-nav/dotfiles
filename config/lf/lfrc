cmd open_terminal &{{
	alacritty --hold $F
}}

cmd open_external_editor &{{
	$EDITOR $f
}}

cmd hide_if_file &{{
    if test -f "$f"; then
	lf -remote "send $id hide"
    fi
}}

cmd hide &{{
    i3-msg [class="scratch"] scratchpad show
}}

cmd open_and_close_scratchpad :hide_if_file; open

cmd open_emacsclient &{{
	pwd | sed -r "s/\s/\\\ /g" | emacsclient .
}}

# cmd done ${{
# 	if test -f "$f"; then
# 		filename=$(basename $f)
# 		echo ${filename}
# 		mv "${filename}" "(D) ${filename}"
# 	fi
# }}

map w :hide; open_terminal
map E :hide; open_external_editor
map c
map U clear

map cc open_codium
map yd $(pwd | sed -r "s/\s/\\\ /g" | xclip -sel clip)
map yD $(pwd | xclip -sel clip)

# map ad done

map q
map <a-q> quit
map <a-d> delete
map ; read 

map gm cd ~/MEGA
map gu cd ~/MEGA/Uni/3YEAR/Q2/
map gd cd ~/dotfiles
map gc cd ~/.config

map <enter> shell

map O $ echo $f | sed -r "s/\s/\\\ /g" | mimeopen --ask 

set drawbox

map l open_and_close_scratchpad
map <right> open_and_close_scratchpad
